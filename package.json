{
  "name": "custom-cookie-cmp",
  "version": "1.3.1",
  "private": true,
  "scripts": {
    "build:css": "cleancss -o assets/css/cookie-cmp.min.css assets/css/cookie-cmp.css",
    "build:js": "terser assets/js/cookie-cmp.js -o assets/js/cookie-cmp.min.js --compress --mangle",
    "build": "npm run build:css && npm run build:js",
    "watch": "onchange 'assets/css/cookie-cmp.css' -- npm run build:css & onchange 'assets/js/cookie-cmp.js' -- npm run build:js"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "onchange": "^7.1.0",
    "terser": "^5.36.0"
  }
}
